# 🚀 دليل الإعداد السريع - نظام إدارة السائقين

## 📋 المتطلبات الأساسية

- حساب على [Supabase](https://supabase.com) (مجاني)
- Node.js 18 أو أعلى
- متصفح حديث

## ⚡ خطوات الإعداد السريع

### 1️⃣ إعداد Supabase

#### إنشاء المشروع
1. افتح [supabase.com](https://supabase.com) وسجل دخول
2. اضغط على "New Project"
3. اختر اسم للمشروع وكلمة مرور قوية لقاعدة البيانات
4. اختر المنطقة الأقرب إليك
5. انتظر حتى يتم إنشاء المشروع (2-3 دقائق)

#### الحصول على مفاتيح API
1. من لوحة تحكم Supabase، اذهب إلى:
   ```
   Project Settings → API
   ```
2. ستجد:
   - **Project URL**: عنوان مشروعك
   - **anon/public key**: المفتاح العام
3. احفظ هذه البيانات للخطوة التالية

### 2️⃣ إنشاء قاعدة البيانات

1. من لوحة Supabase، اذهب إلى:
   ```
   SQL Editor → New Query
   ```

2. انسخ محتوى ملف `database.sql` بالكامل

3. الصق المحتوى في محرر SQL

4. اضغط على "Run" أو `Ctrl/Cmd + Enter`

5. انتظر حتى تظهر رسالة "Success"

✅ **تم!** الآن لديك:
- 7 جداول جاهزة
- علاقات مترابطة
- فهارس للأداء
- بيانات تجريبية للاختبار

### 3️⃣ إعداد التخزين للمستندات

1. من لوحة Supabase، اذهب إلى:
   ```
   Storage → Create a new bucket
   ```

2. أنشئ bucket باسم: `driver-documents`

3. اختر:
   - ✅ Public bucket (للوصول السهل للصور)
   - المدة الزمنية للـ Cache: 3600 ثانية

4. اضغط "Save"

### 4️⃣ تكوين المشروع المحلي

1. انسخ ملف البيئة:
   ```bash
   cp .env.example .env.local
   ```

2. افتح `.env.local` وعدّل:
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
   ```

3. ضع مفاتيح Supabase التي حصلت عليها في الخطوة 1

### 5️⃣ تشغيل المشروع

```bash
# تثبيت المكتبات
npm install

# تشغيل الخادم التطويري
npm run dev
```

افتح المتصفح على: **http://localhost:3000**

## 🎨 البيانات التجريبية

بعد تنفيذ `database.sql`، ستجد:

### السائقون (3)
- Ahmed Ali Mohamed (نشط)
- Mohamed Hassan Ibrahim (تدريب)
- Khaled Ibrahim Ahmed (نشط)

### الشركات (2)
- Company A Ltd (تسعير لكل طلب)
- Express Delivery Co (تسعير لكل stop)

## 📊 الميزات الجاهزة

✅ **Dashboard**: إحصائيات حية وتقارير مرئية
✅ **إدارة السائقين**: إضافة، تعديل، عرض ملفات كاملة
✅ **إدارة الشركات**: تسعير مرن وإعدادات مخصصة
✅ **صفحة السائق**: KPIs، الطلبات، الرواتب، السلف، المستندات

## 🔐 الأمان

### تفعيل Row Level Security (اختياري)

لتأمين البيانات، يمكنك تفعيل RLS:

```sql
-- في SQL Editor
ALTER TABLE drivers ENABLE ROW LEVEL SECURITY;
ALTER TABLE companies ENABLE ROW LEVEL SECURITY;
ALTER TABLE orders ENABLE ROW LEVEL SECURITY;
ALTER TABLE weekly_salaries ENABLE ROW LEVEL SECURITY;
ALTER TABLE advances ENABLE ROW LEVEL SECURITY;

-- إضافة سياسة للمشرفين فقط
CREATE POLICY "Enable all for authenticated users" ON drivers
  FOR ALL USING (auth.role() = 'authenticated');
```

## 🐛 حل المشاكل الشائعة

### خطأ في الاتصال بقاعدة البيانات
```
❌ Error: Invalid API key
```
**الحل**: تأكد من نسخ المفتاح بالكامل من Supabase Settings → API

### الصور لا تظهر
```
❌ Error: Storage bucket not found
```
**الحل**: تأكد من إنشاء bucket اسمه `driver-documents` وأنه public

### خطأ في البناء
```
❌ Error: Module not found
```
**الحل**: 
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📱 الخطوات التالية

بعد الإعداد، يمكنك:

1. **تخصيص البيانات**: عدّل البيانات التجريبية أو احذفها
2. **إضافة المصادقة**: ادمج نظام تسجيل دخول للمشرفين
3. **ربط API حقيقية**: استبدل البيانات الوهمية بـ API calls حقيقية
4. **النشر**: انشر على Vercel أو Netlify

## 🚀 النشر السريع على Vercel

```bash
npm install -g vercel
vercel
```

لا تنسَ إضافة متغيرات البيئة في:
```
Vercel Dashboard → Project Settings → Environment Variables
```

## 💡 نصائح مهمة

- 🔑 **لا تشارك** مفاتيح API الخاصة بك
- 📝 احفظ نسخة احتياطية من ملف `.env.local`
- 🔄 راجع Supabase Dashboard بانتظام للتحديثات
- 📊 استخدم Supabase Dashboard لمراقبة الأداء

## 📞 الدعم

إذا واجهت أي مشكلة:
1. تحقق من [Supabase Docs](https://supabase.com/docs)
2. راجع [Next.js Docs](https://nextjs.org/docs)
3. تأكد من تنفيذ جميع خطوات الإعداد

---

**مبروك! 🎉 نظامك جاهز للاستخدام**
