# 🚗 نظام إدارة السائقين - المميزات

## 🎯 المميزات الرئيسية المكتملة

### 1️⃣ لوحة التحكم الرئيسية (Dashboard)
```
✅ إحصائيات في الوقت الفعلي
   ├── إجمالي الإيرادات (Total Revenue)
   ├── إجمالي الرواتب (Total Salaries)
   ├── صافي الربح (Net Profit)
   └── إجمالي الطلبات (Total Orders)

✅ رسوم بيانية تفاعلية
   ├── اتجاهات الإيرادات والأرباح (Line Chart)
   ├── الطلبات الأسبوعية (Bar Chart)
   ├── توزيع الإيرادات حسب الشركات (Pie Chart)
   └── ترتيب أفضل السائقين (Ranking)

✅ إحصائيات ثانوية
   ├── عدد السائقين النشطين
   ├── عدد السائقين في التدريب
   ├── عدد الشركات النشطة
   └── الطلبات المكتملة

✅ آخر النشاطات
   └── تحديثات فورية للعمليات
```

### 2️⃣ إدارة السائقين (Drivers Management)
```
✅ قائمة السائقين
   ├── جدول شامل بجميع البيانات
   ├── بحث سريع (Search)
   ├── تصفية حسب الحالة (Status Filter)
   └── إحصائيات سريعة (Active/Training/Suspended)

✅ معلومات السائق
   ├── الاسم الكامل
   ├── رقم الهاتف
   ├── واتساب (رابط مباشر)
   ├── تاريخ البدء
   ├── الحالة (Training/Active/Suspended)
   ├── الراتب الأسبوعي
   ├── الطلبات المطلوبة
   └── ملاحظات داخلية

✅ المستندات
   ├── الهوية (أمام)
   ├── الهوية (خلف)
   ├── جواز السفر
   ├── معاينة الصور (Image Viewer)
   └── حالة المستندات (Complete/Incomplete)

✅ عمليات CRUD
   ├── إضافة سائق جديد
   ├── تعديل بيانات السائق
   ├── حذف سائق
   └── عرض تفاصيل كاملة
```

### 3️⃣ صفحة ملف السائق (Driver Profile)
```
✅ KPIs حية
   ├── طلبات هذا الأسبوع
   ├── الطلبات المتبقية للهدف
   ├── شريط التقدم (Progress Bar)
   ├── الراتب المتوقع
   ├── الحالة الحالية
   └── السلف النشطة

✅ تبويبات (Tabs)
   ├── Orders: الطلبات الحديثة
   │   ├── تاريخ الطلب
   │   ├── اسم الشركة
   │   ├── العميل
   │   ├── عدد Stops
   │   ├── الإيرادات
   │   └── الحالة
   │
   ├── Salaries: تاريخ الرواتب
   │   ├── رقم الأسبوع والسنة
   │   ├── الفترة (من - إلى)
   │   ├── عدد الطلبات
   │   ├── الراتب الأساسي
   │   ├── البونص
   │   ├── خصم السلفة
   │   ├── الراتب النهائي
   │   └── الحالة (Paid/Pending)
   │
   ├── Advances: السلف
   │   ├── مبلغ السلفة الأصلي
   │   ├── المبلغ المتبقي
   │   ├── تاريخ السلفة
   │   ├── قيمة القسط
   │   ├── عدد الأقساط (المدفوع/الإجمالي)
   │   ├── شريط التقدم
   │   └── ملاحظات
   │
   └── Documents: المستندات
       ├── معاينة الهوية (أمام/خلف)
       ├── معاينة جواز السفر
       ├── تكبير الصورة (Modal)
       ├── رفع مستندات جديدة
       └── حالة الاكتمال
```

### 4️⃣ إدارة الشركات (Companies Management)
```
✅ عرض الشركات
   ├── بطاقات (Cards) احترافية
   ├── بحث متقدم
   ├── تصفية حسب الحالة
   └── إحصائيات (Active/Paused)

✅ معلومات الشركة
   ├── اسم الشركة
   ├── شخص التواصل
   ├── رقم الهاتف
   ├── البريد الإلكتروني
   ├── الحالة (Active/Paused)
   ├── نوع التسعير (Per Order/Per Stop)
   ├── قيمة السعر
   ├── تعليمات ثابتة
   └── ملاحظات

✅ إعدادات الطلبات
   ├── ☑️ Require Post Code
   ├── ☑️ Require Customer Name
   ├── ☑️ Require Phone
   ├── ☑️ Require Stops
   └── ☑️ Require Delivery Notes

✅ عمليات CRUD
   ├── إضافة شركة جديدة
   ├── تعديل بيانات الشركة
   ├── حذف شركة
   └── عرض تفاصيل
```

## 🎨 المكونات المشتركة (Reusable Components)

### Sidebar
```typescript
- تصميم حديث ومرن
- قائمة تنقل كاملة
- أيقونات واضحة
- حالة نشطة للصفحة الحالية
- متجاوبة (Mobile Menu)
- معلومات المستخدم
```

### Header
```typescript
- شريط بحث عام
- زر الإشعارات
- التاريخ الحالي
- تصميم ثابت (Sticky)
```

### StatsCard
```typescript
- عرض الإحصائيات
- أيقونات ملونة
- دعم الاتجاهات (Trends)
- ألوان قابلة للتخصيص
- رسوم متحركة
```

### Modal
```typescript
- نوافذ منبثقة احترافية
- أحجام متعددة (sm, md, lg, xl, 2xl)
- إغلاق سهل
- Backdrop شفاف
- متجاوبة
```

### LoadingSpinner
```typescript
- 3 أحجام (sm, md, lg)
- رسوم متحركة سلسة
- تكامل مع التصميم
```

## 🧮 المنطق التجاري (Business Logic)

### حساب الراتب الأسبوعي
```javascript
// القواعد:
IF السائق في فترة التدريب (أول أسبوعين):
    الراتب = £0

ELSE IF عدد الطلبات < 100:
    الراتب = £0
    يتطلب قرار إداري (Waive)
    
ELSE IF عدد الطلبات = 100:
    الراتب = £120

ELSE IF عدد الطلبات > 100:
    الراتب = £120 + (عدد_الطلبات - 100) × £1
    البونص = عدد_الطلبات - 100

// مثال:
// 105 طلب = £120 + £5 = £125
```

### حساب الإيرادات
```javascript
IF نوع_التسعير === "per_order":
    الإيرادات = السعر_لكل_طلب

ELSE IF نوع_التسعير === "per_stop":
    الإيرادات = السعر_لكل_stop × عدد_stops

// مثال شركة A (per_order):
// سعر الطلب = £15
// الإيرادات = £15

// مثال شركة B (per_stop):
// سعر ال stop = £5
// عدد stops = 3
// الإيرادات = £5 × 3 = £15
```

### خصم السلف
```javascript
IF لدى_السائق_سلفة_نشطة:
    قيمة_الخصم = MIN(قيمة_القسط, الراتب_النهائي)
    الراتب_بعد_الخصم = الراتب_النهائي - قيمة_الخصم
    المبلغ_المتبقي = المبلغ_المتبقي - قيمة_الخصم

// لا يمكن أن يتجاوز الخصم قيمة الراتب
```

## 📊 قاعدة البيانات

### الجداول (7 Tables)
```sql
1. drivers              -- بيانات السائقين
2. companies            -- الشركات المتعاقدة
3. company_order_settings -- إعدادات الطلبات
4. orders               -- سجل الطلبات
5. weekly_salaries      -- الرواتب الأسبوعية
6. advances             -- السلف
7. advance_payments     -- دفعات السلف
```

### العلاقات
```
drivers (1) ←→ (∞) orders
drivers (1) ←→ (∞) weekly_salaries
drivers (1) ←→ (∞) advances

companies (1) ←→ (∞) orders
companies (1) ←→ (1) company_order_settings

advances (1) ←→ (∞) advance_payments
weekly_salaries (1) ←→ (∞) advance_payments
```

## 🎨 التصميم

### نظام الألوان
```css
Primary (الأزرق):    #0284c7
Success (الأخضر):    #16a34a
Warning (البرتقالي):  #d97706
Danger (الأحمر):     #dc2626
```

### المتجاوبية
```
Mobile:  < 640px
Tablet:  640px - 1024px
Desktop: > 1024px

✅ جميع الصفحات متجاوبة 100%
```

## 📚 التوثيق

```
├── README.md            -- دليل شامل (EN)
├── SETUP_AR.md          -- دليل الإعداد (AR)
├── DEVELOPER_GUIDE.md   -- دليل المطور
├── PROJECT_SUMMARY.md   -- ملخص المشروع (AR)
├── FEATURES.md          -- قائمة المميزات (AR)
└── database.sql         -- سكريبت قاعدة البيانات
```

## 🚀 الأداء

```
✅ بناء ناجح (npm run build)
✅ لا توجد أخطاء TypeScript
✅ تحميل سريع
✅ رسوم متحركة سلسة
✅ تجربة مستخدم ممتازة
```

## 🔐 الأمان

```
✅ متغيرات البيئة (.env.local)
✅ TypeScript للأمان
✅ Validation جاهزة
✅ Supabase RLS جاهزة
✅ تخزين آمن للمستندات
```

---

## 📊 الإحصائيات النهائية

| العنصر | العدد |
|--------|------|
| الصفحات الكاملة | 4 |
| المكونات | 5 |
| الجداول | 7 |
| الملفات | 27 |
| الأسطر | 10,000+ |
| نسبة الإكمال | ~70% |

---

**🌟 نظام احترافي متكامل جاهز للاستخدام والتطوير!**
